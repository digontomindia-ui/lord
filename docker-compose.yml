version: '3.8'

services:
  mongodb:
    image: mongo:latest
    volumes:
      - mongo_data:/data/db
    networks:
      - lords_network
    restart: unless-stopped
    expose:
      - "27017"

  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    environment:
      - PORT=5000
      - NODE_ENV=production
      - MONGODB_URI=mongodb://mongodb:27017/tailor_erp
      - JWT_ACCESS_SECRET=lords_bespoke_jwt_access_secret_production_key_2026
      - JWT_REFRESH_SECRET=lords_bespoke_jwt_refresh_secret_production_key_2026
    depends_on:
      - mongodb
    networks:
      - lords_network
    restart: unless-stopped
    expose:
      - "5000"

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=/api
    expose:
      - "80"
    depends_on:
      - server
    networks:
      - lords_network
    restart: unless-stopped

volumes:
  mongo_data:
    driver: local

networks:
  lords_network:
    driver: bridge
